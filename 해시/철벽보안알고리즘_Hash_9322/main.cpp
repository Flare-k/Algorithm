#include <iostream>
using namespace std;
/*
한 단어는 1~10개의 대문자들로 이루어진 문자열, 한 문장은 공백으로 구분된
단어들로 이루어짐 제 1 공개키는 최대 한 번만 사용된 단어들 제 2 공개키는 제 1
공개키의 단어들을 재배치하여 만들어진다.

*/
void init() {
   ios_base::sync_with_stdio(false);
   cin.tie(0);
   cout.tie(0);
}

int main() {
   init();

   return 0;
}